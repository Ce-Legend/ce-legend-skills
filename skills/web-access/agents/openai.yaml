interface:
  display_name: "Web Access"
  short_description: "分层处理搜索、页面读取、浏览器操作、登录态和来源核验"
  default_prompt: "Use $web-access to verify live web information, cite sources, and operate browser flows when needed."

policy:
  allow_implicit_invocation: true
