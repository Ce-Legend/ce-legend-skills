interface:
  display_name: "Web Access"
  short_description: "Codex 外部事实访问层：搜索、核验、浏览器操作与证据链"
  default_prompt: "Use $web-access to verify live web information, preserve source evidence, and operate browser flows when needed."

policy:
  allow_implicit_invocation: true
